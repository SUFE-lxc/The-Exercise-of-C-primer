a) 
错误，copy函数要求目标序列至少包含和原序列一样多的元素，而vec没有size即也没有元素
b)
错误，即使已经分配了10个int，但还是没有元素，fill_n不能添加元素

特点：算法总是通过迭代器操作容器，无法改变size以及insert或者delete元素
但可以使用back_inserter(vec)，插入迭代器可以调动底层容器操作。即能不能插入和删除关键在于传给算法的迭代器的能力。
